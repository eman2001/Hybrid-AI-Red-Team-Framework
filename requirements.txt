# ─────────────────────────────────────────────────────────────
# Hybrid AI Red Team Simulation Framework
# Requirements — UCAS Cyber Security Engineering 2026
# ─────────────────────────────────────────────────────────────

# ── Core Framework ────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
python-dotenv==1.0.1

# ── Database ─────────────────────────────────────────────────
sqlalchemy==2.0.30
aiosqlite==0.20.0          # async SQLite driver
# psycopg2-binary==2.9.9   # uncomment for PostgreSQL

# ── Machine Learning ─────────────────────────────────────────
scikit-learn==1.4.2
numpy==1.26.4
pandas==2.2.2
joblib==1.4.2

# ── XGBoost / LightGBM (optional, comment out if not needed) ─
# xgboost==2.0.3
# lightgbm==4.3.0

# ── Explainability ───────────────────────────────────────────
# shap==0.45.0              # uncomment for SHAP explanations

# ── Attack Graph ─────────────────────────────────────────────
networkx==3.3

# ── Network Scanning ─────────────────────────────────────────
python-nmap==0.7.1

# ── STIX / MITRE ─────────────────────────────────────────────
stix2==3.0.1               # optional: parses STIX bundles natively
requests==2.32.2

# ── Reporting ────────────────────────────────────────────────
jinja2==3.1.4
# reportlab==4.1.0          # uncomment for PDF generation
# weasyprint==62.3          # alternative PDF engine

# ── Logging / Monitoring ─────────────────────────────────────
structlog==24.1.0
python-json-logger==2.0.7

# ── Testing ──────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0              # async HTTP client for API tests

# ── Development ──────────────────────────────────────────────
python-multipart==0.0.9    # required by FastAPI for form uploads

# ─────────────────────────────────────────────────────────────
# Installation:
#   pip install -r requirements.txt
#
# For ML models only:
#   pip install scikit-learn numpy pandas joblib networkx
#
# For full API stack:
#   pip install fastapi uvicorn sqlalchemy python-nmap
# ─────────────────────────────────────────────────────────────
